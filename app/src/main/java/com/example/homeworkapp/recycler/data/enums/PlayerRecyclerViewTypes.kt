package com.example.homeworkapp.recycler.data.enums

enum class PlayerRecyclerViewTypes(val tapeValue: Int) {
    PLAYLIST_TYPE(tapeValue = 1),
    CATEGORIES_TYPE(tapeValue = 2),
    NEW_REC_TYPE(tapeValue = 3),
}